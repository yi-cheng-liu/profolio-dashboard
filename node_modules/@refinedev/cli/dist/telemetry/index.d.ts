import type { NODE_ENV } from "@definitions/node";
import type { ProjectTypes } from "@definitions/projectTypes";
interface TelemetryData {
    nodeEnv?: NODE_ENV;
    nodeVersion: string;
    os: string;
    osVersion: string;
    command: string;
    packages: {
        name: string;
        version: string;
    }[];
    projectFramework: ProjectTypes;
}
export declare const getTelemetryData: () => Promise<TelemetryData>;
export declare const telemetryHook: () => Promise<void>;
export {};
//# sourceMappingURL=index.d.ts.map